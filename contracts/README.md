** Mixer **

The mixer is a contract that allows users to deposit and withdraw funds privately.

The mixer uses a verifier contract to verify the proofs of the users.

The mixer uses a merkle tree to store the commitments of the users.
